---
id: yaks-3677
title: TUI doc screenshots via headless buffer->SVG (color)
type: feature
priority: 3
created: '2026-09-06T23:02:22Z'
updated: '2026-09-06T23:41:29Z'
parent: yaks-012b
labels:
- ui
- docs
---

toque emits style-encoded TEXT, not images. Build a small buffer->SVG renderer (drive App headlessly via render_to_buffer / a Session to a target state; walk the Buffer's per-cell symbol+fg/bg -> SVG grid with colored text + bg rects). SVG embeds in markdown with color. Relates to yaks-2892 (rendered Buffer + avt).

---
▸ 2026-09-06T23:11:52Z [coordinator]
FEASIBILITY CONFIRMED (not yet built). The render path is proven: src/tui/headless.rs already drives App headlessly via toque (render_to_buffer / Session) in tests. The only new piece is a buffer->SVG FORMATTER (walk the ratatui Buffer's per-cell symbol + fg/bg/modifier -> SVG grid: bg <rect>s + colored monospace <text>, bold via font-weight; map ratatui Color -> hex). SVG embeds in markdown WITH color. WRINKLE (flagging early per Joel): yaks is a BIN crate (no lib target), so an examples/ file can't 'use yaks::tui::App'. Cleanest home: a test-gated doc-asset generator (a #[test]/ignored fn, like the existing headless tests, that writes docs/assets/*.svg) or a hidden 'yaks tui --render' path. Plan: test-based generator driving a small sample herd to a few states (list, detail w/ needs badge, inbox, filter drawer) -> SVGs -> embed in docs/tui.md. No blocker found.

---
▸ 2026-09-06T23:41:26Z [coordinator]
Built the buffer->SVG renderer as a test-gated generator: src/tui/docshots.rs (a #[cfg(test)] child module of tui, #[ignore]d 'docshots' test). Walks the ratatui Buffer -> rounded backing rect + per-cell bg rects + per-run colored <text> with textLength pinning so alignment survives the viewer font. Tomorrow-Night palette; ratatui Color->hex incl. Rgb passthrough + full xterm-256 indexed. Drives a small sample herd to 4 states (Shaving list, HITL detail with Needs + [wtA]/[joel] notes, Inbox, filter drawer) -> docs/assets/*.svg, embedded in docs/tui.md. Regen: cargo test -p yaks docshots -- --ignored. Verified visually via rsvg-convert.
