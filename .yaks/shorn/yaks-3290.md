---
id: yaks-3290
title: slice the farm by herd (filter + TUI badge/current-herd)
type: feature
priority: 3
created: '2026-09-21T17:32:32Z'
updated: '2026-09-21T19:19:35Z'
parent: yaks-15f7
depends_on:
- yaks-4154
labels:
- ui
---

Make a multi-herd farm legible. Add a herd/prefix filter to the shared FilterSpec (list/search/next/tangled); note that --search PREFIX already substring-matches ids as a stopgap. In the TUI: a per-row herd badge derived from the id prefix (Task.id already carries it, no model change), optional group or scope by herd, and a current herd used for creation. Filter flag spelling pends the docs/naming child.

---
▸ 2026-09-21T19:19:35Z [agent]
SHORN (core scope). Multi-herd legibility + create routing. (1) CLI/query: repeatable --herd <prefix> filter on the shared FilterSpec (list/search/next/tangled + bulk selector), matching on id prefix; persisted in saved views and preserved by the drawer. (2) TUI create routing: a context-seeded target herd - the parent's herd for a child create, else the selected row's - shown in the create header ('New yak · herd: X') and passed as the new yak's prefix; gated to multi-herd farms (>1 distinct prefix) so single-herd TUI/snapshots are unchanged. First consumer path for Config::known_herds() semantics. Tests: filter herd-predicate + two tui create-routing tests; suite green (255 lib + 25 cli), warning-free. Verified on a real multi-herd farm: list --herd web scoped; --herd web --herd api ORed; headless create header read 'New yak · herd: api'. DEFERRED to a follow-up: an explicit per-row herd BADGE and an in-form herd PICKER (the id already surfaces the herd via its prefix, and the in-form chip needs invasive create-form row surgery); context-seeded current-herd covers the shared-farm create need meanwhile.
