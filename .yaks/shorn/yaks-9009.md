---
id: yaks-9009
title: List-view scrolling pins to bottom
type: bug
priority: 3
created: '2026-08-24T19:22:37Z'
updated: '2026-09-23T21:37:30Z'
labels:
- ui
---

When scrolling the list view up from the bottom, it always pushes the selected item to the bottom of the viewport.
Instead, it should be scrolling no more than necessary to keep the selected item in view.

---
▸ 2026-09-23T21:35:37Z [coordinator]
Lightning-round lane 'scroll' (worktree wt/scroll, branch lane-scroll). Scope: list/tree viewport offset logic (minimal scrolling to keep selection visible). Evidence contract: gate = cargo test -p yaks green (re-accept + eyeball any snapshot ripple); PLUS drive the headless TUI (toque) to the relevant state and yaks attach the frame (text, or docshot SVG when color matters). Judge: coordinator at merge review; subjective forks -> yaks ask and hand back.

![yaks-9009-scroll-evidence](artifacts/yaks-9009/yaks-9009-scroll-evidence.txt)

---
▸ 2026-09-23T21:37:30Z [lane-scroll]
Headless before/after frames (60x16, 30 rows): G then kkk. Before: cursor pinned to bottom edge; after: viewport stays put, cursor moves up within it.

---
▸ 2026-09-23T21:37:30Z [lane-scroll]
Shorn: render_list rebuilt ListState from offset 0 every frame, so ratatui scrolled only enough to show the cursor *from the top* — pinning it to the bottom edge when moving up. Fix: App.list_offset: Cell<usize> carries the viewport offset across frames (seeded into ListState::with_offset, read back after render). Covers list and tree views (both render via render_list). Regression test tui::tests::list_scroll_up_moves_viewport_minimally (+ snapshot). cargo test -p yaks green (281 + 25). No key/behavior docs to change.
