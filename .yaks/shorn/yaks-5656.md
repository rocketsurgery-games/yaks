---
id: yaks-5656
title: Ref autocomplete while editing
type: feature
priority: 3
created: '2026-08-29T22:13:46Z'
updated: '2026-08-30T00:55:36Z'
parent: yaks-688d
labels:
- links
- jui
---

Trigger yak-id completion while editing a description/comment, so ids do not have to be copy-pasted or memorized (the original driver behind yaks-688d).

Design sketch: reuse the existing FuzzyPick overlay (ranked id/title search, single-line query) with a new FuzzyAction::InsertRef; bind a trigger key in the multiline editor to open it, seeded from the word under the cursor; on commit, insert the FULL prefix-1234 id at the cursor (feed chars through edtui the Insert handler so cursor + undo behave). Full-form insertion matters because validation-based linking keys off the complete id.

NOTE the real cost: overlays are a single enum slot (Overlay::…), so opening the picker from inside Overlay::Edit needs overlay NESTING (or a stack) to return to the editor afterward — that architectural bit is the bulk of the work, which is why this is split out of yaks-c2c5 rather than folded in.

Split from yaks-c2c5. Detection/highlight/follow already shipped there; the resolver (yaks-0187) provides the candidate set + full-form ids.

---
▸ 2026-08-29T22:50:26Z
Manual trigger landed + the nesting plumbing (the hard part), all tested. FuzzyAction::InsertRef; FuzzyPick gains return_to: Option<Box<Overlay>> so a picker opened over the editor restores it on commit OR cancel. Ctrl-Space in an Insert-mode editor opens the reparent-style FuzzyPick; Enter inserts the picked full id at the cursor via insert_str (replays Insert-mode keystrokes so edtui advances cursor + records undo); Esc/Ctrl-C return to the editor unchanged. 2 tui tests. 194 lib tests, warning-free.

REMAINING (the user preferred UX): auto-open on typing "<prefix>-". Small on top of this plumbing, but needs (a) reading the edtui cursor/token before it (token_before_cursor over state.cursor + lines), (b) a replace_len on FuzzyPick so committing deletes the typed "<prefix>-" before inserting the full id (avoids prefix-prefix-1234), (c) firing ONLY when the token becomes exactly "<prefix>-" so it is not obtrusive mid-token, with Esc-to-dismiss (already wired). 

SPIN-OFF: yaks-63f3 (open yak by id) is now nearly free — same FuzzyPick with a FuzzyAction::OpenById that jumps to the task detail (return_to=None), opened from the list.

---
▸ 2026-08-29T23:56:47Z
TRIGGER FIX: dropped Ctrl-Space (unreliable — legacy terminals send it as a NUL/Ctrl-@ that crossterm does not report as Char(space)+CTRL, and yaks only enables the Kitty protocol at DISAMBIGUATE_ESCAPE_CODES, which does not distinguish Ctrl-Space; would need REPORT_ALL_KEYS, not worth flipping globally). Replaced with the auto-trigger the user originally wanted: completing "<prefix>-" while inserting opens the picker; picking replaces the typed "<prefix>-" with the full id, Esc/Ctrl-C keeps it. Reliable (plain printable keys). Plumbed herd prefix into App.ref_prefix (from cfg.prefix; default "yak"); FuzzyPick.replace_len; token_before_cursor + delete_back helpers over edtui cursor/lines. 2 tests rewritten (prefix-dash inserts + replaces; cancel keeps prefix). Also covers title search: after "<prefix>-" the picker query matches id OR title.

---
▸ 2026-08-30T00:10:18Z
ROOT CAUSE of "yaks- does nothing": the trigger was wired only into the comment editor (Overlay::Edit). Descriptions + new yaks are edited in the CreateForm (Overlay::Create, handle_create_key), a SEPARATE key path that forwarded to f.blocks[i].editor with no trigger. So M (comment) triggered; E/c (edit/create description) did not.

FIX: factored the insert/delete/token helpers to be surface-agnostic (operate on &mut EditorEventHandler + &RefCell<EditorState>), generalized open_ref_picker to stash ANY editor overlay, and taught commit_fuzzy InsertRef to insert into either Overlay::Edit(ed) or the active Overlay::Create content block. Wired the same "<prefix>-" detection into handle_create_key. Regression test added for the create-form path. Verified end-to-end on the real herd via the headless driver: typing yaks- in a comment shows overlay=fuzzy + "Insert yak reference (20 matches)". 195 lib tests, warning-free.

STILL OPEN: pick a manual trigger (auto-<prefix>- works but requires typing the prefix). Terminal-friendly + intuitive options being proposed to the user.

---
▸ 2026-08-30T00:33:17Z
Esc UX: loosened picker dismissal — once the query is in Normal mode, a single further Esc closes it (esc_from_normal), no rapid double-Esc. Ctrl-C still cancels from anywhere. Test: single_esc_from_normal_closes_the_picker (clears the double-Esc timer to isolate the path). 196 lib tests.

---
▸ 2026-08-30T00:49:34Z
Reworked to the opt-in hint+Tab model (per user):
- Removed the auto-popup. Now: typing "<prefix>-" surfaces a help-bar hint "Tab -> complete yak · Esc dismiss" (render_status, unified for the comment editor AND the create/edit description block); nothing pops up until you press Tab.
- Tab-to-complete is context-scoped (completion_context: token before cursor starts with "<prefix>-"). Picker opens seeded by the tail already typed (yaks-ab -> filtered to "ab"); commit replaces the whole "<prefix>-…" token.
- Tab now behaves NORMALLY inside content/description blocks (no more next-field); Ctrl-N/P are the field-nav backup. Updated the one live test that walked blocks via Tab to use Ctrl-N.
- Esc: single Esc from Normal closes the picker (no rapid double-Esc).
Tests: tab_after_prefix replaces token; tab_in_content_block does not navigate; create-form trigger; cancel keeps prefix; single_esc closes. 197 lib + 20 CLI, warning-free. Verified headless on the real herd: yaks- shows the hint (overlay stays edit), Tab opens the picker.
