---
id: yaks-7511
title: Add an 'init' command to create a new .yaks/ herd
type: feature
priority: 2
created: '2026-08-23T02:09:20Z'
updated: '2026-09-02T21:16:41Z'
labels:
- cli
---

MISSING TOOL discovered during skills reconstruction: the Rust CLI has no 'init' (store::discover_root only walks up and errors if none found), so there is no way to start a fresh herd — the yak skill can't tell a user how to begin one. Port the Python 'yaks init': create .yaks/{hairy,shaving,shorn,dead}/ + a config.yaml (prefix/default_type/default_priority/vim_mode). Then the skill's Running-yaks section can point at it.

---
▸ 2026-09-02T21:16:41Z
Implemented 'yaks init': store::init() scaffolds .yaks/{hairy,shaving,shorn,dead}/ + config.yaml (prefix/default_type/default_priority/vim_mode) + schema marker; refuses to clobber an existing .yaks/ (InitOutcome::AlreadyExists). CLI: 'yaks init' with --prefix/--type/--priority/--emacs, handled before Herd::open like 'skills'. Added 2 store tests (round-trips through read_config; no-clobber). Updated bundled skills/yak/SKILL.md Running-yaks section + commands table to point at init.
