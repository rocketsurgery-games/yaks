---
id: yaks-1603
title: Extract resolve_actor into a shared module (CLI + TUI)
type: task
priority: 3
created: '2026-09-03T17:59:19Z'
updated: '2026-09-03T18:23:31Z'
parent: yaks-594b
labels:
- cli
- ui
---

resolve_actor (--as > $YAKS_ACTOR > git user.name) currently lives in main.rs. The TUI needs the same logic to auto-attribute comments (yaks-... auto-attribute). Move it to a shared module (e.g. src/actor.rs or herd) with one implementation and tests, so both surfaces agree. Small enabler; do before the TUI auto-attribute yak.

---
▸ 2026-09-03T18:23:31Z [coordinator]
BUILT. Moved resolve_actor -> src/actor.rs as actor::resolve, with a testable precedence core resolve_with(explicit, env, git_fn) so the --as > $YAKS_ACTOR > git user.name order is unit-tested without touching env/git (test explicit_beats_env_beats_git). main.rs's three call sites (update/ask/answer) now call actor::resolve; old fn removed. Ready for the TUI to reuse (yaks-ffa5). 208 tests green.
