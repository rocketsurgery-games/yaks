---
id: yaks-1699
title: 'TUI slice 3b: shared edtui editor overlay (labels L, create c/C, body edit E)'
type: task
priority: 3
created: '2026-08-21T00:08:18Z'
updated: '2026-08-21T00:50:32Z'
parent: yaks-86a3
labels:
- rust
- phase2
---

Text-input mutations. Add a single-line prompt overlay (a small line editor reproducing Python edit_prompt/input_prompt: cursor, backspace, left/right, Enter=commit, Esc=cancel). Wire L labels (comma-split edit of current labels -> update add/remove), c create (prompt title, then type picker t/b/f/i, create as root hairy), C create-child (same, parent = selected id). Reload + notification after each. Snapshot the prompt overlay and a created row.

---
▸ 2026-08-21T00:41:08Z
Merged 3c into this slice per decision: adopt edtui as the single shared text-input primitive (single-line for labels/create-title, multi-line for body). Verified edtui 0.11.7 rides ratatui-core 0.1.0 / ratatui-widgets 0.3.0 — same as our ratatui 0.30, no duplicate core, so EditorView renders into our Frame. Added lean (default-features=false; drops arboard/syntax-highlighting/mouse). Editor convention: keys flow to edtui (full vim/emacs incl Esc->Normal in multi-line); Ctrl-S=commit, Ctrl-C=cancel everywhere; single-line adds Enter=commit + Esc=cancel. Wire: L labels (single-line, comma-split -> update add/remove), c/C create (title single-line -> type picker -> create root/child), E edit body (multi-line -> update description). Editor state RefCell-wrapped to keep render(&App) pure.

---
▸ 2026-08-21T00:50:32Z
Done. Added edtui 0.11.7 (default-features=false) as the single shared text-input primitive; confirmed it rides the same ratatui-core 0.1 / ratatui-widgets 0.3 / crossterm 0.29 as ratatui 0.30 (no dup core, KeyEvent types match). New Overlay::Edit(Editor) holds a RefCell<EditorState> + EditorEventHandler so render(&App) stays pure. Wired L (single-line labels -> add/remove diff), c/C (title field -> type picker -> create root/child, cursor lands on new task), E (multi-line body -> update description). Config gained vim_mode (Herd::config()); editor starts in Insert. Convention: Ctrl-S commit / Ctrl-C cancel everywhere; single-line also Enter=commit, Esc=cancel; all other keys flow to edtui (full vim/emacs). Overlays now swallow Ctrl-C (editor cancel) rather than quitting. 59 tests (was 51): 3 editor-render snapshots + live create/child/cancel/labels/body harness. Warning-free.
