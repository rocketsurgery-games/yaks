---
id: yaks-7371
title: 'merge/import: jam two farms together safely'
type: feature
priority: 2
created: '2026-09-21T17:32:32Z'
updated: '2026-09-21T18:08:54Z'
parent: yaks-15f7
labels:
- cli
---

Requested tool to consolidate separate farms into one. Move all task files from farm B into farm A with global id uniqueness guaranteed. Reconcile colliding prefixes first via existing rename-prefix; hard-check bare id collisions across the union before writing; --dry-run preview like rename. Relates to yaks-54e9 (export/import JSON): same move-yaks-between-farms need, decide whether merge subsumes it or rides on it. Preserve status (subdir) and all frontmatter and body verbatim.

---
▸ 2026-09-21T18:08:54Z [agent]
SHORN. Added 'yaks merge <path> [--dry-run]' to consolidate one farm into another. Farm::merge walks the source status dirs (stem=id), refuses WHOLESALE on any id collision against the destination (MergeOutcome::Collision -> reconcile with rename-prefix), else copies each yak file verbatim into the matching status subdir and copies artifacts/<id>/ alongside. Non-destructive (source left intact); resolve_farm_root accepts a .yaks dir or a dir containing one; guards against merging a farm into itself. New MergeOutcome/MergePlan; CLI reports plan (dry-run) / applied / collision with a rename-prefix hint. 3 farm tests (disjoint copy preserves status + non-destructive; collision refuses and writes nothing incl. the non-colliding id; dry-run writes nothing); suite green (248 lib + 25 cli). Verified on scratch farms: dry-run preview, apply (a core farm gains a web herd, source intact), re-merge refuses (exit 1). Relates to yaks-54e9 (export/import JSON): this direct FS merge covers same-machine consolidation; 54e9 remains for serialized/cross-machine moves. Config is NOT merged (destination config governs; source prefix survives as the ids' prefix) - per-herd config is yaks-98d5.
