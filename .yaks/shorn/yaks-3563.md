---
id: yaks-3563
title: Agent skill updates for yak references + rename
type: chore
priority: 3
created: '2026-08-29T18:37:13Z'
updated: '2026-08-29T22:15:41Z'
parent: yaks-688d
depends_on:
- yaks-c2c5
- yaks-7a92
labels:
- links
---

Update the agent skills so agents actually use the reference + rename tooling this herd builds. Currently NEITHER the in-repo skills/ nor the global yak skill mentions inter-yak references at all.

Two surfaces stay in sync: in-repo skills/{yak,yak-tracker}/SKILL.md (source that syncs out) and the global ~/.agents/skills/yak copy.

Content:
- Highest-leverage nudge (from c2c5): always write the FULL prefix-1234 form in titles/descriptions/notes, never bare 1234, so the validation-based resolver links it out of the box.
- Keep guidance prefix-agnostic: "this herd config prefix", not a hardcoded yaks-/yaksrs-.
- Document the new verbs once built: rename a yak, rename the config prefix (with --dry-run), and how to address a yak in another herd (qualified form, yaks-15f7).
- Note the follow/autocomplete affordances in the TUI.

Depends on the feature yaks it documents (yaks-c2c5, yaks-7a92).

---
▸ 2026-08-29T22:15:41Z
SHORN. Added to skills/yak/SKILL.md: a "Referencing other yaks" section + "Renaming safely" subsection, and the three new commands (refs, rename, rename-prefix) in the Commands table. Core guidance: always write the FULL prefix-1234 form (never bare 4-hex); prefix-agnostic ("this herd config prefix", not hardcoded); [[wiki]] works; TUI highlights + follows mentions; `yaks refs` for integrity; rename/rename-prefix with --dry-run.

NOTE: the GLOBAL ~/.agents/skills/yak copy is stale — it still documents the old Python `uvx yakherder` packaging and has diverged from this repo source. Left it untouched (outside the project); it should be re-synced from skills/yak/SKILL.md. yak-tracker skill not touched (external-tracker scope, unaffected).
