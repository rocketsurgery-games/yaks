---
id: yaks-3901
title: Yaks as a coordination substrate for agents and humans (design exploration)
type: idea
priority: 2
created: '2026-08-30T17:57:39Z'
updated: '2026-09-02T23:00:45Z'
labels:
- agent
---

Root of the exploration herd for using yaks as an unopinionated coordination substrate for agents and humans, across harnesses (Pi minimal, Claude Code rich, Zed panel). Goal is primitives and high-leverage CLI/TUI affordances, not a prescribed methodology. Skills stay no more prescriptive than necessary.

Design tenets
- Files authoritative, index derived (existing invariant).
- Same substrate for agents and humans. Both read and write it.
- Methodology-agnostic. Yaks supplies primitives (state, parentage, deps, refs, labels, notes/comments, source). Workflows compose on top. We do not bake one in.
- Portable across harnesses. A minimal harness uses yaks as durable memory and log. A rich harness uses it as a shared blackboard across subagents. Degrade gracefully. Assume nothing about subagents or worktrees.
- Evidence over assertion (from pstack). A yak is not shorn without recorded evidence in its notes.
- Keep the state machine tiny. Push coordination nuance to labels, fields, and comments, not new states.

Exploration areas (candidate child subtrees)
1. Coordination patterns. Blackboard via parent/child + deps + notes. Claim via shave. One writer per yak. Human-in-the-loop validation channel. Fan-out research and summarization (parent is the question, children are slices, notes are summaries). Lightweight planning as a tree of yaks.
2. Query affordances (read side). Comments and notes since a timestamp across a filter. Recency and activity feeds. Rich JSON that exposes notes, timestamps, verdicts.
3. Bulk and write affordances. Bulk label and metadata edits by id-list or dynamic filter. Bulk state transitions. Filter-driven mutations with dry-run.
4. Metadata for coordination. Which labels, fields, and conventions carry coordination signal. Note authorship and agent attribution. Verdict and evidence fields as a convention.
5. Harness integration. Pi vs Claude Code mapping. MCP surface. Per-repo agent context. The degradation model.
6. Meta and evaluation. Dogfood on this herd. Measure whether an affordance cuts turns and herding drudgery before promoting it.

Related existing hairy yaks to fold in, not duplicate
- Bulk and filtered updates: yaks-5fae, yaks-8d53, yaks-9ccc, yaks-de85.
- Agent-facing coordination: yaks-b517, yaks-2c16, yaks-7cd1, yaks-5335.
- Eval and testing harness: yaks-1c5c, yaks-d487, yaks-c35f.
- Metadata surface: yaks-4b52, yaks-d695, yaks-640a, yaks-f04d.
- Query and search: yaks-71c2, yaks-953f, yaks-158d.
- CLI plumbing: yaks-54e9, yaks-aa49, yaks-c53a, yaks-2120.

Working mode. Create children as we commit to a thread. Prefer the smallest tool or skill change that tests an idea. Slice this subtree with --parent-of on this root rather than a new label.

---
▸ 2026-09-02T23:00:45Z
[coordinator] Consolidated state + decisions (pre-compaction brief).

ARCHITECTURE DECISION: file-based is the TARGET (tasks as greppable/diffable/editable files, status as directory), NOT git-store. git-store (git-bug op-log in refs) is a different product, superior only in the private+distributed+offline+multi-writer quadrant; parked as research in yaks-4fe6 with an incremental migration path if ever pursued.

COORDINATION CONSENSUS: per-branch worktree herds, refined toward a Model 1/3 hybrid: workers write only their own leaf yaks (disjoint -> clean merge + free provenance + isolation); the coordinator solely owns shared/parent yaks, integration, and PRs. Rule OUT Model 2 (workers writing a shared root: not portable under harness isolation). Model 3 (coordinator sole writer) is the reliable fallback. Disjoint assignment is the rule; 2-4 agents. Merge driver (yaks-74f5) + doctor (yaks-39d4) are a safety net, not a dependency.

TENETS: files authoritative / index derived; one substrate for agents+humans; methodology-agnostic primitives, skills no more prescriptive than necessary; portable across harnesses (Pi <-> Claude Code), degrade gracefully; evidence over assertion (no shear without recorded evidence); tiny state machine (push nuance to labels/notes). Design test: expressible as a CLI query/mutation over files -> yaks core; assumes subagents/worktrees -> skill/adapter.

VERIFIED: the full worktree flow works (disjoint scopes, coordinator merge, clean reconciliation, provenance dogfooded). GOTCHA: agent file tools root at the MAIN checkout, not the terminal cwd -> edit worktree files via the nested .worktrees path and verify against the worktree binary (Zed/spawn_agent-specific; encoded in the coordinating-yaks skill). Per-branch herds reconcile at merge, not live. Observability is after-the-fact; the yak trail IS the record.

SUPPORTING DECISIONS: provenance = recover from git, no stored hashes (yaks commits, DONE). PR = coordinator opens id-free PRs from worker branches; ids in commit messages OK, never in PR text or external trackers; squash needs ids in the squash message, the git-log --follow join is the durable one (yaks-213b). Attribution not ownership; --as actor optional, git author is free for committed transitions (yaks-eb66). Public/private herd patterns + skill/docs/CLI updates (yaks-c4b2).

STATE. Shorn: yaks-c9ee (log --since), yaks-2610 (commits), yaks-9dfe (coordinating-yaks skill). Shaving: yaks-012b (skills/dev seed, pending commit), yaks-8f81 (dogfooding + worktrees). Open: yaks-2ebe (write-side bulk ops, next feature, solo worktree), yaks-8265 (transition-timestamp log), yaks-74f5 (merge driver), yaks-39d4 (doctor), yaks-eb66 (attribution), yaks-213b (PR-driven integration), yaks-c4b2 (public/private).

SEQUENCING TO START: (1) measure-first experiment: two workers touch the SAME parent yak on plain files, observe the merge, to decide whether yaks-74f5 is even needed; (2) yaks-c4b2 skill/docs update (pure docs, immediate); (3) yaks-2ebe write-side bulk ops in a solo worktree; then motivated primitives yaks-eb66 and yaks-8265; safety nets yaks-74f5 / yaks-39d4 only if the collision test warrants; yaks-213b when moving past local merges.
