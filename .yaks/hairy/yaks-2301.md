---
id: yaks-2301
title: b1cc/5 Extract render layer -> src/tui/render/
type: task
priority: 3
created: '2026-09-11T01:29:48Z'
updated: '2026-09-11T03:16:27Z'
parent: yaks-b1cc
depends_on:
- yaks-3294
labels:
- ui
needs: human
verify: cargo test -p yaks
---

Move render root + render_tabs/render_list/render_detail/render_status/render_view_picker/render_help + helpers (right_divider/help_content/priority_style/status_word/disp_width/format_count/truncate_disp/dedent/detail_scan/filter_summary/help_hint/pinned_header_line/list_item/render_dline) into src/tui/render/ (mod + list/detail/status split). Move tests + snapshots. Green.

---
▸ 2026-09-11T01:39:46Z [coordinator]
Phase-5 decision (provenance): render_detail placement. There is already src/tui/detail.rs that builds the detail MODEL (DLine). Where does the RENDER fn go? (a) keep model/render split -- model stays in detail.rs, painting goes in render/detail.rs (mirrors how tree.rs works); or (b) fold the renderer into the existing detail.rs. Coordinator lean: (a) -- keep 'build the model' and 'paint it' separate. Your call.

---
▸ 2026-09-11T01:47:59Z [Joel Webber]
(a) works for me.

---
▸ 2026-09-11T03:16:27Z [coordinator]
Snapshot organization decision (the tidy-up you flagged). Finding: all 22 insta snapshots are whole-FRAME integration renders driven through the top-level render()/draw() (create_form, *_picker_overlay, filter_drawer, detail_*, tree_, status_*, list, help, etc.) -- most assert several modules' output at once, not one module in isolation. So how should they land after the render split? (a) Keep them CENTRAL: render_* code moves to src/tui/render/, but the whole-frame snapshot + integration tests stay in ONE integration-test home (tui.rs mod tests, or a dedicated src/tui/tests.rs) with .snaps in src/snapshots/. Honest (they ARE whole-app tests), one clear home, low risk. (b) MIGRATE per-module: push each snapshot to the module it most tests (create_form->create, filter_drawer->drawer, etc.), .snaps move to per-module snapshots/ dirs. Strict co-location, but multi-concern frames get assigned somewhat arbitrarily, and it's a larger, riskier move+rename. Coordinator lean: (a) -- integration snapshots want a single integration home; strict per-module co-location fights their whole-frame nature. Your call (a/b).

---
▸ 2026-09-11T03:16:27Z [coordinator]
Provenance link: this decides how the src/snapshots/ sprawl you raised at the arc's start gets tidied. Whatever you pick, tests/snapshots/ (CLI) is unaffected -- only cli__headless_session is TUI-adjacent and correctly stays with the CLI tests.
